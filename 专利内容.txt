





本发明公开了一种多变量时间序列缺失数据插补方法，其特征在于包括：第一步，采集含有缺失值的多变量时间序列数据 ；第二步，将原始不完备数据进行多次随机掩码处理，通过深度自编码器得到多掩码特征，并设计一致性损失函数，使得不同掩码视图下的特征在潜在空间中保持对齐 ；第三步，分别在时间和空间维度上建立邻域保持模块，在空间领域上基于完备数据集有效观测维度的最近邻样本计算空间权重并提取空间压缩特征，在时间领域上基于完备数据集有效观测维度的最近邻变量计算时间权重并提取时间压缩特征 ；第四步，构建自适应融合网络对模型进行训练，生成动态权重对特征进行融合并优化参数 ；第五步，利用训练好的模型对测试集数据进行测试，通过一次前向传播输出最终的缺失值填补结果 。
本发明公开一种多变量时间序列缺失数据插补方法。步骤一：采集含缺失的数据X并生成二值掩码M，缺失位置归零得观测矩阵，并将观测矩阵与掩码拼接输入网络，同时计算整体缺失率ρ。步骤二：训练阶段执行多掩码一致性去噪，在观测位置随机再丢弃生成多份增强掩码，分别编码得到多视图潜在表示，以“损坏越重权重越小”的一致性损失约束表示对不同缺失模式稳定。步骤三：提出时空邻域保持嵌入结构，在潜在空间中以时间步与变量为节点构建双域邻域；相似度仅在共同观测条目上计算以避免伪相似，并用k近邻与核权重聚合得到空间特征及经“变量级→时间级”映射的时间特征，保持降维后的局部流形一致性。步骤四：将三路特征与ρ输入自适应融合网络生成动态注意力权重，实现随稀疏度自调的加权融合。步骤五：解码输出并仅在缺失位置给出插补值，端到端优化；推理阶段一次前向传播即可完成填补。本发明在复杂缺失下提升插补精度与鲁棒性并保持时空局部结构。













1.一种基于一致性降噪和时空邻域保持嵌入的深度自编码器缺失数据填补方法，其特征在于，包括以下步骤：
步骤S1：获取待处理的包含缺失值的多变量时间序列数据集X和对应二进制掩码矩阵，对缺失位置进行归零处理，并将与沿特征维度拼接得到网络输入；同时计算数据集的整体缺失率；
步骤S2：构建深度自编码器网络，包括编码器、解码器以及嵌入在二者之间的相干去噪模块、空间流形模块、时间流形模块和自适应融合模块；
步骤S3：利用编码器对步骤S1的输入进行非线性映射压缩成低维度的潜在表示，生成原始潜在特征表示；
步骤S4：在训练阶段，所述相干去噪模块通过对掩码矩阵进行多次伯努利随机损坏生成增强掩码并得到对应潜在表示，构造与损坏程度相关的加权一致性损失，以约束模型对不同缺失模式的鲁棒性。所述空间流形模块与时间流形模块分别采用仅基于共同观测条目的策略计算相似度、近邻确定邻域并采用核函数计算并归一化邻域权重进行加权聚合，生成空间特征与经变量级到时间级映射后的时间特征；
步骤S5：利用所述自适应融合模块，利用GAP归一化与整体缺失率计算注意力权重，分别将、、经变量级到时间级映射后的时间特征 加权融合得到融合特征 。 
步骤S6：利用解码器将映射回原始数据空间以输出重构后的完整数据矩阵，构建仅在观测条目上计算的重构损失、一致性损失、空间保持损失和时间保持损失的联合损失函数，通过梯度下降优化网络参数。
根据权利要求1所述的方法，其特征在于，在步骤S3中，所述编码器通过非线性变换将输入数据映射到潜在空间，具体表示为：

表示哈达玛积，和为权重矩阵，和为偏置向量，为激活函数，表示拼接操作。
3.根据权利要求1所述的方法，其特征在于，在步骤S4中，所述一致性去噪机制包括：
在每次训练迭代中，通过伯努利采样生成 个增强掩码矩阵：
其中 为时间步索引， 为变量索引， 表示增强掩码的索引。
在每个掩码 下，通过编码器得到对应的潜在表示 ，并记 为原始潜在表示 。
构造一致性正则化损失：

其中， 为增强掩码编号， 为原始潜在表示， 为基于损坏严重程度的权重，且随 大而减小；优选地，

其中表示L1范数，为控制带宽参数。
根据权利要求1所述的方法，其特征在于，在步骤S4中：
(1) 空间邻域权重采用核函数计算：
并通过近邻确定空间邻域集合 ，空间特征通过归一化加权聚合得到：
(2) 时间邻域权重采用核函数计算：

其中，和为变量索引，表示仅基于共同被观测时间点计算的欧氏距离；
通过K近邻确定时间邻域集合 ，时间特征 通过对邻域变量的特征表示进行归一化加权聚合及映射得到：
其中， 表示邻居变量 的潜在特征表示。
根据权利要求1所述的方法，其特征在于在步骤S5中，所述自适应融合网络生成注意力权重并进行加权融合的过程表示为：计算融合系数向量：

其中 表示全局平均池化结果， 表示整体缺失率；融合特征为：

中为原始嵌入，为空间流形嵌入，为时间流形嵌入。
根据权利要求1所述的方法，其特征在于，在步骤S6中，所述联合损失函数满足： 

其中重构损失仅在观测条目上计算：其中 为超参数，重构损失 仅在掩码 标示的观测条目上计算：

根据权利要求1所述的方法，其特征在于，在推理阶段不执行所述伯努利随机损坏生成增强掩码及一致性损失计算，仅进行一次前向传播完成缺失值填补。
基于一致性去噪与时空邻域保持嵌入的深度自编码器缺失数据填补方法

技术领域
本发明属于工业/医疗传感器数据处理技术领域，具体涉及一种面向多变量时间序列数据的缺失值填补方法，尤其涉及一种结合一致性去噪与时空邻域保持嵌入的深度自编码器填补方法，可用于工业过程监控、软测量、故障诊断及医疗监测数据的预处理。
背景技术
多变量时间序列数据的完整性是工业过程监控（如化工反应过程、污水处理）与医疗健康监测等领域中的核心基础。从受损或不完整的传感器测量中准确重建缺失的数据，对于实施有效的故障检测、软测量开发及下游优化控制策略至关重要。随着现代工业系统日益向高维化、强非线性及动态时变方向发展，且常受传感器故障、信号传输中断或设备检修维护等不可控因素干扰，鲁棒的缺失数据填补方法的发展变得尤为重要。最根本的挑战在于设计既能适应复杂多变的缺失模式（如非随机缺失），又能在填补过程中保持数据内在物理流形结构的算法。
现有的缺失数据填补方法主要分为传统统计学方法、基于传统机器学习的方法以及基于深度学习的生成式方法三类。传统的统计学方法如均值填补、多重插补通常依赖于强统计分布假设或变量间的线性相关性假设，但在面对具有强非线性耦合、非平稳特性的复杂工业数据时，往往难以捕捉潜在的动态变化规律，导致填补结果偏差较大或丢失关键的高频细节信息。
近年来，基于深度学习的数据驱动方法如生成对抗网络、循环神经网络和去噪自编码器因其强大的非线性映射能力而受到广泛关注。然而，现有的深度学习填补模型往往忽视了数据在潜在空间中的固有流形几何结构，即无法保证重构后的数据点严格分布在真实的物理流形面上；且在缺乏一致性正则化约束的情况下，模型容易对特定的训练掩码分布产生过拟合，导致其在面对训练数据分布之外的复杂缺失模式（如连续块状缺失）时泛化能力较差，这在对数据可靠性与物理一致性要求极高的工业应用中是不可接受的。
现有的时空建模方法虽然试图结合时间依赖性与变量相关性，但现有方法往往缺乏一种统一的机制来同时显式地保持空间（变量维）与时间（序列维）的局部邻域结构，难以在保证数据一致性的同时兼顾拓扑结构的完整性，这可能限制了它们在处理具有复杂耦合关系的一般非线性系统数据时的适用性。

发明内容
本发明采用如下技术方案：
一种基于一致性降噪和时空邻域保持嵌入的深度自编码器缺失数据填补方法，包括以下步骤：
S1：获取待处理的包含缺失值的多变量时间序列数据矩阵以及对应的二值掩码矩阵；对中的缺失位置进行归零处理得到观测矩阵，并将与沿特征维度拼接得到网络输入同时计算数据集的整体缺失率，作为后续自适应融合的先验输入；
S2：构建深度自编码器（AE-CS）网络，其结构包括编码器、解码器以及嵌入在二者之间的相干去噪模块、空间流形模块、时间流形模块和自适应融合模块；
S3：利用编码器对步骤S1所述的网络输入 

 进行非线性映射与特征压缩，生成原始潜在特征表示；
S4：在训练阶段，执行以下并行操作以增强特征的鲁棒性与结构保持能力：
相干去噪：所述相干去噪模块通过对初始掩码矩阵进行多次伯努利随机损坏生成多视图增强掩码，并基于增强掩码生成对应的潜在表示，构造权重与损坏程度自适应相关的加权一致性损失，以约束模型对复杂缺失模式的鲁棒性；
空间流形保持：以时间步为节点，采用部分距离策略（仅基于共同被观测的变量）计算时间步之间的相似度，利用kNN确定空间邻域，并采用核函数计算归一化权重进行聚合，生成空间特征；
时间流形保持：以变量为节点，采用部分距离策略（仅基于共同被观测的时间点）计算变量之间的相似度，利用kNN确定时间邻域并计算归一化权重；由于变量级特征与时间步级特征维度不匹配，需进一步执行“变量级→时间级”嵌入映射，将变量维度的聚合特征映射回时间步维度，生成时间特征；
S5： 利用自适应融合模块，将特征的全局平均池化结果与步骤S1计算的整体缺失率
 共同输入网络，生成动态注意力权重，对、、进行加权融合得到融合特征，从而根据数据稀疏程度自动调整对时空邻域信息的依赖比例；
S6：利用解码器将映射回原始数据空间以输出重构后的完整数据矩阵，构建包含仅在观测条目上计算的重构损失、一致性损失、空间保持损失和时间保持损失的联合损失函数，通过梯度下降算法端到端优化网络参数。


有益效果：
(1) 显著提升了模型对复杂缺失模式的鲁棒性与泛化能力。
本发明引入了相干去噪机制，通过构建多视图掩码一致性约束，迫使模型学习对输入掩码扰动不敏感的鲁棒特征。这种自监督机制有效模拟了随机缺失甚至部分非随机缺失场景，使得模型在面对高缺失率或连续块状缺失时，仍能依据未损坏的上下文信息准确判断缺失值，避免了传统模型对特定掩码分布的过拟合。
(2) 有效保持了多变量时间序列的内蕴几何拓扑结构与物理一致性。
区别于传统仅关注数值重构误差的方法，本发明创新性地设计了空间与时间双域邻域保持模块，并采用基于部分观测距离的度量策略，解决了缺失值干扰相似度计算的难题。这使得填补后的数据在潜在空间中严格遵循原始数据的流形分布，不仅保持了时间上的工况连续性，也保持了变量间的物理耦合关系（如传感器冗余性），从而显著降低了因填补导致的结构扭曲对下游任务（如故障检测、软测量）精度的负面影响。
(3) 实现了针对不同数据质量场景的自适应动态填补。
通过引入结合整体缺失率先验的融合机制，模型能够“感知”当前数据的稀疏程度：在数据完整度高时优先保留原始特征细节，在数据极度稀疏时自动切换为依赖历史相似工况（空间流形）和相关传感器信息（时间流形）进行推理。这种机制保证了算法在不同工业场景下的通用性和有效性。
(4) 兼顾了训练的充分性与推理的高效性。
虽然在训练阶段引入了复杂的多掩码增强与流形约束以保证模型性能，但在在线推理阶段，无需执行随机损坏及一致性计算分支，仅需一次前向传播即可输出填补结果，计算复杂度低，满足工业过程监控对实时性的严苛要求。

附图说明
为了更清楚地说明本发明实施例或现有技术中的技术方案，下面将对实施例描述中所需要使用的附图作简要介绍。显而易见地，下面描述中的附图仅仅是本发明的一些实施例，对于本领域普通技术人员来讲，在不付出创造性劳动的前提下，还可以根据这些附图获得其他的附图。
图1为本发明实施例的基于一致性去噪与时空邻域保持嵌入的深度自编码器缺失数据填补方法的流程图。

具体实施方式
使本发明的上述目的、特征和优点能够更加明显易懂，下面结合附图对本发明的具体实施方式做详细说明。在下面的描述中阐述了很多具体细节以便于充分理解本发明。但是本发明能够以很多不同于在此描述的其它方式来实施，本领域技术人员可以在不违背本发明内涵的情况下做类似改进，因此本发明不受下面公开的具体实施例的限制。
此外，术语“第一”、“第二”仅用于描述目的，而不能理解为指示或暗示相对重要性或者隐含指明所指示的技术特征的数量；限定有“第一”、“第二”的特征可以明示或者隐含地包括至少一个该特征；在本发明的描述中，“多个”的含义是至少两个，除非另有明确限定。
实施例1
如图1所示，本实施例提供了一种基于一致性去噪与时空邻域保持嵌入的深度自编码器缺失数据填补方法，该方法包括以下步骤：
S1：数据与掩码获取、预处理
S1：获取待处理的包含缺失值的多变量时间序列数据矩阵 

 和对应的初始二值掩码矩阵 
其中，矩阵元素 表示第个时间步第个变量的观测值存在，表示缺失（其中 ，）。
将中缺失位置归零，得到观测矩阵，其中表示哈达玛积。
进一步地，为便于网络训练，对每个变量进行归一化处理；优选地，采用基于观测值的均值方差归一化，参数仅由的条目计算得到。
更进一步地，计算整体缺失率，用于后续自适应权重生成网络的输入，计算公式为：

S2：构建深度自编码器网络（AE-CS）
S2：构建基于一致性降噪与时空邻域保持嵌入的深度自编码器网络。该网络采用端到端的层次化特征提取架构，主要包含编码器、解码器，以及嵌入在二者潜在空间的并行特征处理分支：相干去噪模块、空间流形模块、时间流形模块和自适应融合模块。
具体的网络配置如下：
编码器： 用于将高维的输入数据（观测数据与掩码的拼接，维度为2N）非线性映射到低维潜在空间。
结构上，优选采用多层全连接网络，也可扩展为一维卷积神经网络。
网络层数设置为 L层，通过层级降维强制网络学习数据的压缩表示。层与层之间采用 ReLU作为激活函数 ，以引入非线性变换并缓解梯度消失问题；进一步地， 训练过程中可结合梯度裁剪技术以防止梯度爆炸，并采用早停策略以防止过拟合。
并行处理分支：
相干去噪分支： 仅在训练阶段激活，通过伯努利采样生成多视图增强掩码 
时空流形分支： 分别基于变量维度和时间维度的邻域拓扑结构对潜在特征进行聚合；
自适应融合模块： 构建网络，由全连接层与 Softmax 归一化层组成，用于动态输出各分支的注意力权重。
解码器： 其结构与编码器呈镜像对称，用于将融合后的潜在特征  映射回原始数据空间，输出维度为N的重构矩阵。
S3：拼接缺失指示器并编码得到原始潜在表示 
S3：将观测矩阵与掩码沿特征维度拼接作为编码器输入，此时输入特征维度由 N 变为 2N
以区分“真实零值”与“缺失导致的归零”， 即：

中“”表示拼接操作。编码器输出原始潜在表示：


具体的，若采用两层非线性网络，编码过程可表示为：

其中 为ReLU激活函数， 为可训练参数。
S4：训练阶段的相干去噪 + 时空邻域保持嵌入
S4：在训练阶段，执行相干去噪过程并构造一致性损失；同时执行时空邻域保持嵌入，生成空间特征 与时间特征 。
相干去噪模块：多掩码伯努利损坏与一致性损失
本模块旨在通过构建自监督信号，利用“多视图学习”策略强迫编码器捕捉数据中对缺失模式不敏感的鲁棒特征，从而提升模型在面对任意缺失模式（包括随机缺失及连续块状缺失）时的泛化能力。
在模型训练的每一次迭代中，不仅使用原始掩码进行训练，还通过随机损坏生成个增强掩码视图
具体过程如下：
对于第个视图（），通过伯努利采样生成一个与数据维度相同的随机损坏矩阵。矩阵中每个元素独立服从伯努利分布：
其中，为时间步索引，为变量索引，为预设的丢弃概率（即人为引入缺失的概率），将随机损坏矩阵作用于原始掩码，得到增强掩码：

技术原理说明
由于采用逐元素乘法（），生成的增强掩码必然是原始掩码的子集。这基于一个关键逻辑：即只能在“已观测”的数据上进行进一步的“伪人为缺失”操作，而无法对原本就缺失的数据进行标记。虽然训练过程中采用的是独立随机丢弃，但这种高强度的随机性迫使模型学习数据间的长程依赖，从而间接赋予模型处理工业场景中常见的连续“块状缺失”的能力。
在每个掩码 下，通过编码器得到对应的潜在表示 ，并记 为原始潜在表示 。
并计算增强掩码对应的潜在表示：

当 时对应原始掩码 ，其输出记为 。

构造一致性正则化损失：

其中， 为增强掩码编号， 为原始潜在表示.
 为基于损坏严重程度的权重，且随 增大而减小；优选地，

进一步地，所述 可设置为 ， 可设置为 ，以在鲁棒性与训练开销之间折中。
空间流形模块：基于观测子集的相似度 + kNN邻域 + 核权重归一化聚合
该模块旨在保持不同时间步（样本）之间的几何结构，使得相似工况的时间步在潜在空间中保持邻近。
首先，需要为每个锚点时间步
（）确定其空间邻域集合。具体地，采用K近邻算法进行搜索。尽管工业多变量时间序列数据的维度可能较高，但受限于物理约束，数据实际分布在低维流形上，因此基于欧氏距离的KNN搜索在捕捉工况相似性方面依然有效。
为保证缺失数据下距离计算的可行性与准确性，本实施例定义如下：
对任意时间步 ，其观测变量集合为 。
对任意两个时间步 ，其共同观测集合为 。
具体的，基于共同观测集合定义归一化距离：

并由高斯核计算亲和权重：

其中为空间核带宽参数，用于控制权重的衰减速度。
然后，采用k近邻策略，从所有的时间步中，选取空间亲和权重（对应最小距离）最大的个索引作为锚点时间步的空间邻域集合。
最后，利用选定的邻域权重，对邻居的原始潜在表示进行加权聚合，得到空间流形嵌入：

由此得到 。
进一步地， 可设置为 ， 可由距离的中位数或经验值确定。
（三）时间流形模块：跨变量的相似度 + kNN邻域 + 聚合，并映射为时间特征矩阵
本实施例旨在捕捉不同变量（传感器）之间随时间演化的相似性。基于流形假设，工业系统中不同传感器之间往往存在物理耦合或冗余关系（例如，反应釜内部温度与夹套温度在时间轴上的波动趋势高度一致）。
特别说明：在本模块的公式表述中，角标含义发生转换，指代当前锚点变量（列索引），指代邻居变量（列索引）。
第一步：基于观测子集的变量间相似度计算
为了解决缺失值对相关性计算的干扰，采用部分距离策略。
定义每个变量的有效观测时间集合：

定义任意两个变量和的共同观测时间集合：


基于共同观测时间点，计算锚点变量与目标变量之间的归一化欧氏距离：



(注：公式中表示时间步 第个变量的观测值)
第二步：确定时间邻域与亲和权重
基于上述距离，利用高斯核函数计算变量与之间的特定时间亲和权重：

其中为时间核带宽参数。采用K近邻策略，选取权重最大的个变量作为锚点变量的时间邻域集合。
第三步：变量级嵌入映射
由于编码器输出的原始潜在表示是针对时间步的行向量（），而上述计算得到的是变量间的列相关性（），存在维度失配问题。为了将变量间的耦合关系融入到时间步特征中，本实施例设计了如下“变量级→时间级”映射过程：
构造变量级潜在表示：
首先，将原始潜在表示从时间维度聚合到变量维度。对于每个变量，计算其所有有效观测时间步的特征均值，得到该变量的独立特征表示：


变量级邻域聚合：
利用第二步计算得到的亲和权重，在变量特征空间中进行加权聚合，利用邻居传感器（如夹套温度）的信息来增强锚点传感器（如反应釜温度）的表示，得到变量级时间流形嵌入：


映射回时间步特征矩阵：
最后，将聚合后的变量级特征重新映射回每个时间步，以生成可与
 融合的时间特征矩阵。对于任意时间步，其时间特征由该时刻所有有效观测变量的变量级嵌入均值构成：
最终得到对齐后的时间流形特征矩阵 。
S5：自适应融合模块（网络输出权重，softmax归一化）
S5：本步骤旨在建立一个自适应融合机制，根据数据的整体稀疏程度和特征的全局分布，动态调整原始特征、空间流形特征和时间流形特征在最终填补中的贡献比例。
1. 特征压缩与全局上下文提取
首先，对步骤S3得到的原始潜在表示、步骤S4得到的空间流形特征和时间流形特征分别进行全局平均池化（GAP），以压缩时间维度信息，获得反映全局特征分布的上下文向量：

2. 自适应权重网络生成融合权重
构建一个可学习的自适应权重生成网络，其输入不仅包含上述三个上下文向量，还显式地拼接了步骤S1中计算得到的整体缺失率。这是为了使网络能够感知当前数据的“可信度”。
通过线性变换与激活函数，计算三路特征的归一化融合系数向量：
其中：表示特征拼接操作；和分别为网络的权重矩阵和偏置向量，是训练过程中可学习的参数；函数确保了。
3. 加权融合与动态调节机制
基于计算得到的权重，对三路特征矩阵进行逐元素加权求和，得到最终的融合特征：

本实施例引入缺失率参与权重计算的技术原理在于实现了如下动态调节逻辑：
当数据较为完整时（较小）：包含丰富且准确的原始信息，网络通过学习倾向于输出较大的，使模型优先保留原始数据的细节特征；
当数据极度稀疏时（较大，例如）：此时中包含大量填充的零值或噪声，信息量极低。自适应权重生成网络会自动增大和的值，迫使模型利用依赖
（历史时刻的相似工况）和（相关变量的耦合关系）来推断缺失值，从而保证填补结果在极端缺失下的可靠性。
S6：解码重构、输出填补结果与联合损失优化
S6：解码器输出重构矩阵：

进一步地，为了确保原始观测数据的真实性不被模型的近似误差改变，最终的填补结果采用掩码融合策略：

即：对于掩码为1的位置保留原始观测值，仅在掩码为0的缺失位置填充模型的预测值。
构建重构损失（仅在观测条目上计算），迫使潜在空间包含尽可能多的细节以恢复原始值：

构建空间保持损失与时间保持损失：
空间保持损失：
利用步骤S4（2）中计算的空间亲和权重，约束原始潜在表示在时间维度上的局部几何结构：
该损失项作为正则化项，试图让相似工况的潜在表示在空间上更加平滑、聚集。
时间保持损失：
利用步骤S4（3）中计算的时间亲和权重，约束变量级潜在表示的耦合关系：
其中为步骤S4中定义的变量级潜在表示。该损失项迫使具有强相关性的传感器（如互为冗余的测点）在潜在空间中保持接近。
联合损失函数：
其中为超参数。
本实施例设置上述超参数的策略基于以下梯度流博弈分析：
重构损失试图最小化误差，倾向于保留所有高频细节，但可能导致对噪声的过拟合；
流形损失（）试图拉近邻居距离，倾向于使潜在空间变得平滑，但过度的平滑会导致所有嵌入趋同，失去样本间的区分度。
因此，通过调节，在“重构保真度”与“流形平滑度”之间寻找纳什均衡点，既保证填补准确性，又维持数据的物理拓扑结构。

通过梯度下降或Adam等优化算法对参数 进行更新，直至 收敛。
训练过程：具体的，模型训练过程包括：
步骤1：初始化编码器、解码器、自适应网络及相关参数；
步骤2：按批次输入训练数据，执行步骤S3~S6计算 与各损失项；
步骤3：对网络进行反向传播计算梯度；
步骤4：采用优化算法更新网络参数，直至验证集损失不再下降或达到预设迭代次数，训练结束，得到训练完成的缺失数据填补模型。
推理过程：在推理阶段，为提高运行效率，不执行伯努利随机损坏生成增强掩码及一致性损失计算，仅执行一次前向传播：输入 → 编码得到 → 计算 → 融合得到 → 解码输出 → 得到 。
实施例2
为了说明本发明方法在工程数据上的可实施性，可选取某工业过程监控或医疗监测数据集作为实施例。设数据包含 个时刻、 个变量，将原始数据按时间顺序划分训练集与测试集（例如按 7:3 划分）。通过随机缺失或块缺失方式构造缺失掩码 。






























以上所述实施例的各技术特征可以进行任意的组合，为使描述简洁，未对上述实施例中的各个技术特征所有可能的组合都进行描述，然而，只要这些技术特征的组合不存在矛盾，都应当认为是本说明书记载的范围。
以上所述实施例仅表达了本申请的几种实施方式，其描述较为具体和详细，但并不能因此而理解为对申请专利范围的限制。应当指出的是，对于本领域的普通技术人员来说，在不脱离本申请构思的前提下，还可以做出若干变形和改进，这些都属于本申请的保护范围。因此，本申请专利的保护范围应以所附权利要求为准。







图 1：AE-CS模型流程图














=== 表格 ===
客户案号	
我方案号	
来案名称	一种基于一致掩码与时空邻域保持的深度缺失数据补全方法
专利名称（必填）	一种基于一致掩码与时空邻域保持的深度缺失数据补全方法
专利申请类型（必填）	发明         实用新型          外观设计
	
客户名称	湖南工商大学
技术负责人	姓名：
技术负责人	电话/邮箱：
	
负责代理师	姓名 
负责代理师	电话/邮箱 
初稿时间	2025年  月  日
备注	



	



